## 1. Review and safety baseline

- [x] 1.1 Record clean feature-worktree status and commit the completed OpenSpec artifacts as the review baseline.
- [x] 1.2 Create an isolated review worktree from the review baseline and run all six `review-this` judges against the proposal, design, specs, and current developer tooling.
- [x] 1.3 Synthesize consensus with file/line evidence; resolve P0 contracts for DB isolation, protected state, ownership, Java coverage, provider registry, JSON reports, execution policy, setup, and all-mode truthfulness; rerun strict OpenSpec validation and commit the reviewed plan.

## 2. Repository code-quality skill

- [x] 2.1 Initialize `.codex/skills/code-quality` with the official skill-creator script, repository-local UI metadata, and one-level references.
- [x] 2.2 Write the concise core workflow plus shared, Python, Shell, C++, Java, and frontend references covering hard blockers, judgment heuristics, architecture ownership, data safety, tests, and completion evidence.
- [x] 2.3 Add the skill trigger and unified check/fix completion contract to `AGENTS.md` without weakening existing worktree, review, testing, commit, or merge rules.
- [x] 2.4 Run `quick_validate.py`, verify every referenced file exists, and forward-test Python, Java, and mixed-language tasks with independent agents; fix any trigger or workflow gaps.
- [x] 2.5 Commit the validated skill and agent-policy unit with its forward-test evidence.

## 3. Quality planner, runner, and CLI

- [x] 3.1 Add a focused `trade_py.devtools.quality` package with typed step/result/report models, provider registry, ownership manifest loader, branch-aware scope, planner, bounded executor, and text/JSON renderers.
- [x] 3.2 Implement NUL-safe committed/staged/unstaged/untracked selection, merge-base provenance, byte-bounded batches, option termination, symlink/containment checks, ownership exclusions, and uncovered-source failure.
- [x] 3.3 Implement providers for shared/config, Python, Shell, C/C++, Java/Maven, and frontend checks/fixes with tool/version probes, setup hints, native exit mapping, prerequisites, timeouts, output caps, resource classes, and offline policy.
- [x] 3.4 Implement dependency-aware execution, bounded lightweight concurrency, heavy-step serialization, process-group timeout handling, source-change detection, failure aggregation, and stable `2 > 1 > 0` exit precedence.
- [x] 3.5 Implement concise text plus deterministic versioned JSON reports, stable check IDs, provenance/scope fingerprints, exclusions, bounded diagnostics, remediation codes, and non-executing plan output.
- [x] 3.6 Add thin pre-DB `trade dev check` and `trade dev fix` delegation, route the shell wrapper through frozen/no-sync execution, preserve existing `trade dev quality`, and fix/test review repository-root and stale-worktree validation.
- [x] 3.7 Add temporary-repository and fake-executor tests for scope variants, unsafe filenames/symlinks, ownership, uncovered sources, mixed routing, no changes, missing tools, prerequisites, timeout/signal, read-only protected state, explicit fix, JSON, and mixed exit precedence.
- [x] 3.8 Run focused quality/CLI/lazy-loading/no-DB tests and compileall, then commit the validated runner/CLI unit.

## 4. Language tooling and configuration

- [x] 4.1 Add version-bounded Ruff and BasedPyright development dependencies, make canonical Python setup install them, add conservative configuration, and update `uv.lock` without changing runtime dependencies.
- [x] 4.2 Add frontend Prettier/ESLint dependencies, configs, and scripts while preserving strict TypeScript and production build behavior; update package-lock deterministically.
- [x] 4.3 Add pinned Maven formatter/test policy for `engine/tradedb-driver` with explicit offline setup and module tests.
- [x] 4.4 Add versioned `quality.toml`, repository `.editorconfig` and `.clang-format`, first-party/vendor/generated/fixture/lock/runtime classifications, suppression metadata, tool discovery, and exact setup diagnostics.
- [x] 4.5 Add config/lock consistency and command contract tests proving checks never carry source-write/network flags, fixes only target selected owned files, and vendor/generated/runtime paths are never mutated.
- [x] 4.6 Run changed-file Python, Shell, frontend, Java, and C++ fixture/tool smokes; record unavailable host-tool behavior and commit the language-tooling unit.

## 5. Integration, migration evidence, and closeout

- [x] 5.1 Run non-executing text/JSON plans, the changed-file hard gate, and hard-failing `./trade dev check --all`; publish exact all-mode legacy debt without blanket suppressions or claiming a failed audit passed.
- [x] 5.2 Run focused pytest, CLI compatibility/lazy-loading/no-DB tests, source-only Python syntax validation, frontend typecheck/build, Shell syntax/ShellCheck, Java/Maven tests, relevant CMake/ctest, and strict OpenSpec validation without touching protected data.
- [x] 5.3 Add a synthetic large-path scope test and performance budgets for no-op Git subprocess count, discovery time/complexity, path batching, and bounded executor behavior; record performance-smoke coverage as added.
- [x] 5.4 Run `git status -sb` and `git diff --check`, stage only intentional skill/config/code/test/spec files, and confirm no local data, DB, cache, build, node_modules, or generated artifacts are tracked.
- [x] 5.5 Commit the final validated integration unit, push after the repository-mandated commit cadence, and report exact validations, compatibility, all-mode debt, missing system tools, and remaining risks.
