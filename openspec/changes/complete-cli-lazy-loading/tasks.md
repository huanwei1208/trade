## 1. Recover and Complete CLI Isolation

- [x] 1.1 Recover the coherent nine-module lazy-import WIP without the unrelated main-worktree files
- [x] 1.2 Replace root module-object dispatch with selected-domain absolute dynamic imports
- [x] 1.3 Parse evaluation and run help before runtime imports and make evaluator imports command-specific
- [x] 1.4 Restore runtime-resolvable evaluation/KG annotations and narrow the K-line settings fallback boundary
- [x] 1.5 Separate canonical research invocation from deprecated top-level warning/prog behavior

## 2. Regression Coverage

- [x] 2.1 Add fresh-process tests for top-level/domain import isolation and zero help-path data writes
- [x] 2.2 Add canonical-versus-legacy research warning and dynamic alias routing tests
- [x] 2.3 Add focused coverage for runtime type hints and K-line dependency failure behavior

## 3. Local State Hygiene

- [x] 3.1 Add narrow ignore rules for `.nvim`, repository-local generated DB state, and legacy generated parquet
- [x] 3.2 Confirm local `pyrightconfig.json` is not a valid project gate and keep it out of commits with an explicit residual note

## 4. Validation and Delivery

- [x] 4.1 Run focused CLI/import tests and verify direct module/script help compatibility
- [x] 4.2 Run full pytest, compileall, OpenSpec strict validation, and working-tree diff checks
- [x] 4.3 Measure startup/import-count sanity and classify performance-smoke coverage
- [x] 4.4 Stage only intentional files, commit validated units, push the feature branch, and record compatibility risks
- [x] 4.5 Squash-merge validated data and CLI branches into master, push master, and verify local runtime state remains uncommitted
