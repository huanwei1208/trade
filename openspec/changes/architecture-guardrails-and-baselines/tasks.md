## 1. Governed Design and Baseline Evidence

- [ ] 1.1 Run the diagnostic child design check, resolve all deterministic findings, and record the source-only scope, legacy table classifications, native binding facts, and pointer facts in the governed artifacts. Objective: establish a reviewable guardrail design. Inputs: parent architecture design, current source audit, and immutable design policy. Outputs: valid proposal, design, specs, tasks, and Design Quality Brief. Affected contracts: future module dependency and developer quality contracts. Validation: `uv run ./trade dev design-check architecture-guardrails-and-baselines` reports no blockers or warnings. Rollback: revise only child OpenSpec artifacts. Completion evidence: diagnostic report lists the contract profile and no unresolved findings. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:test]

- [ ] 1.2 Run the six-role architecture design review in a dedicated review worktree, synthesize findings, resolve every P0, and record a digest-bound approval. Objective: challenge prospective scope and prevent legacy facts from becoming unproven ownership claims. Inputs: frozen child artifacts and current source references. Outputs: `design-review.toml` with six approved roles and finding dispositions. Affected contracts: static dependency and baseline inventory boundaries. Validation: review evidence contains source citations, consensus, P0/P1/P2 disposition, and strict approval prerequisites. Rollback: update artifacts and repeat review when the digest changes. Completion evidence: all P0 findings are resolved and every material P1 is resolved or accepted with an owner. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

- [ ] 1.3 Run strict child design approval before implementation. Objective: bind implementation to the current reviewed artifact generation. Inputs: design-review attestation and immutable policy. Outputs: a passing strict design report. Affected contracts: all child guardrail requirements. Validation: `uv run ./trade dev design-check architecture-guardrails-and-baselines --strict` returns zero. Rollback: do not implement; correct governed artifacts and repeat the review if strict fails. Completion evidence: strict report contains the current date, policy digest, artifact digest, and approved six-role record. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

## 2. Source-Only Guard and Baseline

- [ ] 2.1 Create and test the declarative architecture baseline parser/validator for package roots, DDL source facts, table classifications, native binding facts, and compatibility-pointer facts. Objective: make legacy facts auditable without opening data. Inputs: `trade_py/db/trade_db.py`, `trade_py/db/migrations.py`, `trade_py/db/pipeline_db.py`, BTC store source, and CMake binding source. Outputs: versioned baseline file and source-only validator. Affected contracts: architecture baseline inventory. Validation: focused pytest uses temporary source trees plus the repository baseline and asserts malformed, missing, duplicate, unsafe, candidate, and deferred entries fail explicitly. Rollback: remove only the baseline, validator, and test files; application behavior remains unchanged. Completion evidence: every declared fact resolves to its named source literal and no checker path opens a database or artifact. [validates:architecture.baseline] [validation:test]

- [ ] 2.2 Add and test the prospective AST dependency, Cell, contract-leakage, database, Platform-vocabulary, and native-boundary checks for `src/trade` only. Objective: prevent newly introduced target code from recreating prohibited coupling. Inputs: approved graph, baseline classifications, and temporary Python target fixtures. Outputs: architecture check module and deterministic diagnostics. Affected contracts: target Context imports, contracts, adapters, Interfaces, Platform, and Bootstrap. Validation: focused pytest covers allowed/forbidden imports, relative imports, contract leaks, SQL/private-connection escape, Platform vocabulary, and native placement. Rollback: remove only the guard/test additions; retain all legacy sources. Completion evidence: every prohibited fixture fails with a stable rule/path/line and the allowed fixture passes. [validates:architecture.static-guard] [validation:test]

- [ ] 2.3 Register the architecture check as a scoped read-only quality contributor and test plan triggering. Objective: make the guard runnable through existing developer tooling without a separate lint command. Inputs: existing quality registry, models, and contributor pattern. Outputs: one bounded quality step triggered by target source, baseline, or native binding changes only. Affected contracts: `trade dev check` plan/report output. Validation: contributor pytest proves target/baseline/native scope creates the step, legacy-only scope does not, and check mode has no mutation step. Rollback: unregister the contributor and retain source/test baseline files. Completion evidence: `uv run ./trade dev check --show-plan` names the architecture step for the child change. [validates:architecture.static-guard] [validation:test]

## 3. Validation and Delivery

- [ ] 3.1 Run focused architecture tests, Python compile validation, changed-scope quality plan/check, and diff whitespace validation. Objective: verify source-only correctness and quality-runner integration. Inputs: implemented guard, baseline, and tests. Outputs: passing validation evidence. Affected contracts: architecture quality and baseline inventory. Validation: `uv run pytest -q tests/test_architecture_guard.py tests/test_architecture_contributor.py`, `uv run python -m compileall trade_py tests`, `uv run ./trade dev check --show-plan`, `uv run ./trade dev check`, and `git diff --check`. Rollback: do not commit on failures; correct the owning guard/test/baseline source. Completion evidence: all listed commands pass with no data-root, DB, or artifact writes. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:test]

- [ ] 3.2 Run the six-role implementation review in a new review worktree and resolve every P0 before merge. Objective: validate the actual guard semantics, scope, diagnostics, and operational cost. Inputs: frozen implementation diff and focused test evidence. Outputs: six reports and consensus findings. Affected contracts: developer quality and prospective architecture migration. Validation: all six roles inspect actual code, record source citations, and confirm no unresolved P0. Rollback: correct the implementation in this worktree and rerun focused checks/review. Completion evidence: consensus record identifies zero unresolved P0 and any accepted P1/P2 has a follow-up owner. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

- [ ] 3.3 Inspect working-tree safety, commit the validated implementation unit, push the feature branch, and open a PR against `master`. Objective: deliver an isolated, reversible prerequisite for the next child. Inputs: passing validation and review evidence. Outputs: intentional commit, remote branch, and pull request. Affected contracts: none beyond reviewed developer tooling. Validation: `git status -sb` confirms only intended files, commit message records tests/compatibility/no-data-risk and includes the required TRAE CLI co-author trailer, project-local GitHub CLI auth/PR flow succeeds after push. Rollback: revert the isolated commit or close the PR; no runtime/data rollback exists. Completion evidence: commit, push, and PR URL are recorded. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]
