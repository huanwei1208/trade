## 1. Governed Design and Baseline Evidence

- [ ] 1.1 Run the diagnostic child design check, resolve all deterministic findings, and record the source-only scope, independent target source/import roots, canonical scope/filter contract, multi-source table/projection provenance, non-authorizing classifications, producer-derived artifact/pointer/receipt/Capture facts, CLI/HTTP/OpenAPI/SSE source facts, and native binding facts in the governed artifacts. Objective: establish a reviewable guardrail design. Inputs: parent architecture design, current source audit, quality scope/model/planner interfaces, and immutable design policy. Outputs: valid proposal, design, specs, tasks, and Design Quality Brief. Affected contracts: future module dependency and developer quality contracts. Validation: `uv run ./trade dev design-check architecture-guardrails-and-baselines` reports no blockers or warnings. Rollback: revise only child OpenSpec artifacts. Completion evidence: diagnostic report lists the contract and concurrency profiles, bounded developer-tool capacity evidence, and no unresolved findings. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:test]

- [ ] 1.2 Run the six-role architecture design review in a dedicated review worktree, synthesize findings, resolve every P0 and implementation-blocking P1, and record a digest-bound approval. Objective: challenge prospective scope, diagnostic/public-contract behavior, capacity limits, and baseline completeness before legacy facts can become unproven ownership claims or dynamic-execution escapes. Inputs: frozen child artifacts and current source references. Outputs: `design-review.toml` with six approved roles and finding dispositions. Affected contracts: static dependency, shared quality scope, diagnostic envelope, and baseline inventory boundaries. Validation: review evidence contains source citations, consensus, P0/P1/P2 disposition, and strict approval prerequisites. Rollback: update artifacts and repeat review when the digest changes. Completion evidence: all P0/P1 findings are resolved in the artifacts and every accepted P2 has a child or named quality-platform owner. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

- [ ] 1.3 Run strict child design approval before implementation. Objective: bind implementation to the current reviewed artifact generation. Inputs: design-review attestation and immutable policy. Outputs: a passing strict design report. Affected contracts: all child guardrail requirements. Validation: `uv run ./trade dev design-check architecture-guardrails-and-baselines --strict` returns zero. Rollback: do not implement; correct governed artifacts and repeat the review if strict fails. Completion evidence: strict report contains the current date, policy digest, artifact digest, and approved six-role record. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

## 2. Source-Only Guard and Baseline

- [ ] 2.1 Create and test the declarative architecture baseline parser/validator for independent target source/import roots, logical-record classifications, explicit non-authorizing candidate/deferred states, future approved bindings, native binding facts, central and independent schema/projection declarations, producer-derived warehouse artifact declarations, Crypto ADS and BTC pointer/receipt declarations, Kline pointer declarations, precise Capture-risk facts, CLI/HTTP/OpenAPI/SSE source facts, and DDL source provenance roles: `bootstrap`, `migration`, `alter`, and `data_transform`. Objective: make legacy facts auditable without opening data. Inputs: `trade_py/db/trade_db.py`, `trade_py/db/migrations.py`, `trade_py/db/pipeline_db.py`, `trade_py/intelligence/schema.py`, Observatory catalog store, canonical `trade_py.data.warehouse.io.write_table` and `upsert_table`, the streamed bounded tracked production-Python universe beneath `trade_py/`, direct/module imports, aliases, and `trade_py.data.warehouse` package re-exports of those functions, plus data operation checks, root trade/CLI/FastAPI/SSE/contract-test sources, BTC store source, Capture time/catalog/replay/quarantine sources, and CMake binding source. Outputs: versioned baseline file and source-only validator. Affected contracts: architecture baseline and source-only interface inventory. Validation: focused pytest uses temporary source trees plus the repository baseline and asserts malformed, missing, deleted, duplicate, unsafe, incomplete-provenance, candidate, deferred, unapproved-binding, missing producer artifact, an undeclared static production writer including `ods.ods_fetch_attempt`, direct import, package re-export, alias, newly added production writer, declared-writer rename/deletion, and test-only writer exclusion fail or pass explicitly as appropriate. It verifies stable IDs/remediations for unresolved import, layout, nonliteral target, undeclared writer, unsafe source, path-budget, and source-budget failures; streamed raw/included path and source budgets terminate without a partial inventory; and a regular-source control passes while external/data-artifact symlink, non-regular entry, path escape, unavailable no-follow read, and enumeration-to-read replacement all fail `architecture.producer_discovery_unsafe_source`. The fixture permits only the baseline, evidence-source, and repository-confined verified regular descriptors in the bounded production-Python universe; it monkeypatches `sqlite3.connect`, `duckdb.connect`, `pandas.read_parquet`, in-repository data/warehouse/market/SQLite/Parquet/manifest/pointer/receipt reads, and out-of-repository paths to prove no prohibited I/O occurs. Rollback: remove only the baseline, validator, and test files; application behavior remains unchanged. Completion evidence: every declared first-party production call resolving to a canonical warehouse writer with a `WarehouseLayout` first argument resolves to its named source literal, including `dim.dim_data_source` and `ods.ods_fetch_attempt`; no producer inventory is partial; and no checker path opens a database, artifact, non-evidence in-repository path, or out-of-repository path. [validates:architecture.baseline] [validation:test]

- [ ] 2.2 Add and test the prospective AST dependency, Cell, contract-leakage, persistence/artifact-owner, Platform-public-API, legacy-bridge, dynamic-execution, process-spawn, Platform-vocabulary, and native-boundary checks for `src/trade` only. Objective: prevent newly introduced target code from recreating prohibited coupling. Inputs: approved graph, independent import-root declaration, baseline classifications/approved bindings, and temporary Python target fixtures. Outputs: architecture check module and deterministic `trade.architecture.guard.v1` diagnostics. Affected contracts: target Context imports, contracts, adapters, Interfaces, Platform, Bootstrap, and the named Platform persistence legacy-schema bridge. Validation: focused pytest covers allowed/forbidden and relative imports, `src.trade.*` rejection, Platform contracts/api versus adapters, exact Bootstrap-to-Platform bridge allowance, all other `trade_py`/`trade_web` imports, `importlib`/`__import__`/file/native loaders plus `runpy`/`exec`/`eval`/`compile`/`zipimport`, `subprocess`/`os.exec*`/`os.system`/async subprocess/multiprocessing/process-pool calls, contract leaks, SQLite/DuckDB/SQLAlchemy/private-connection/direct-artifact escape, unknown/deferred/candidate/foreign table references, authorized owner adapters, Platform vocabulary, and native placement. It validates required diagnostic fields, ordering/count invariants, invalid-envelope rejection, and a worst-case bounded/truncated serialized envelope. Rollback: remove only the guard/test additions; retain all legacy sources. Completion evidence: every prohibited fixture fails with a stable rule/path/line/remediation; the allowed graph and named bridge fixture pass; each emitted envelope remains within its output limit. [validates:architecture.static-guard] [validation:test]

- [ ] 2.3 Add and test the additive shared quality scope/model/planner contract, then register the architecture check as a scoped read-only contributor. Objective: preserve one canonical Git delta for ordinary filtered execution and fail closed when `--path` hides an architecture-sensitive change, without a contributor Git rescan. Inputs: existing scope selector, `ScopeSelection`, bounded `ProducerDiscoverySelection`, planner/runner plan-issue behavior, registry, batching helpers, and contributor pattern. Outputs: canonical unfiltered modified/added/deleted/rename-source/rename-target/untracked fields, normalized requested filters, bounded producer-candidate/end-point metadata, `architecture.partial_scope` planner result, ordered producer-prefilter steps, one prerequisite baseline validation step, and explicit deterministic target batches. Affected contracts: `trade dev check` scope/plan/report JSON and text output. Validation: scope/planner/contributor pytest proves ordinary providers retain filtered execution fields; target/baseline/native/interface/guard and producer signals trigger; filtered producer modified/add/delete/both rename endpoints/untracked files fail `architecture.partial_scope`; a noncandidate legacy-only scope remains non-triggering; no contributor re-scans Git; `batched_paths()` splits producer and target argv safely; a single untransportable path and over-cap producer selection fail before AST work; deterministic batch IDs preserve deleted/rename metadata; raw/included producer path and 1 MiB/512-file/32-MiB source budgets, four-worker-wave limits, timeout/output configuration, and check mode with no mutation step all hold. Rollback: unregister the contributor and remove the architecture-specific additive scope metadata/planner issue with its source/test baseline files. Completion evidence: `uv run ./trade dev check --show-plan` names producer-prefilter, baseline, and target batches for a complete scope, and reports `architecture.partial_scope` rather than acceptance for an excluded sensitive delta. [validates:architecture.static-guard] [validation:test]

- [ ] 2.4 Add the concise architecture-quality developer runbook. Objective: make bounded diagnostics actionable without creating a runtime operations system. Inputs: stable architecture rule IDs, plan/report CLI behavior, and ownership mapping. Outputs: a developer-facing rule-to-diagnosis/recovery document. Affected contracts: developer remediation guidance only. Validation: focused documentation test or review verifies rule IDs for stale baseline, rename/delete, partial scope, target scope budget, producer unresolved import/layout/nonliteral/undeclared writer, producer path/source budget, producer unsafe source, timeout, invalid envelope, persistence/artifact bypass, and execution bypass map to `trade dev check --show-plan`, JSON report usage, corrective action, rerun command, and escalation owner. Rollback: remove the isolated runbook; runtime behavior remains unchanged. Completion evidence: review evidence confirms every declared operational failure state has one unambiguous remediation owner and command. [validates:architecture.static-guard] [validation:review]

## 3. Validation and Delivery

- [ ] 3.1 Run focused architecture, scope/planner, and contributor tests, Python compile validation, changed-scope quality plan/check, and diff whitespace validation. Objective: verify source-only correctness, canonical partial-scope refusal, bounded diagnostics, and quality-runner integration. Inputs: implemented guard, baseline, shared scope/planner changes, runbook, and tests. Outputs: passing validation evidence. Affected contracts: architecture quality, scope/plan/report, and baseline inventory. Validation: `uv run pytest -q tests/test_architecture_guard.py tests/test_architecture_contributor.py tests/test_quality_scope.py tests/test_quality_planner.py tests/test_quality_executor.py`, `uv run python -m compileall trade_py tests`, `uv run ./trade dev check --show-plan`, `uv run ./trade dev check`, and `git diff --check`. Rollback: do not commit on failures; correct the owning guard/scope/planner/test/baseline source. Completion evidence: all listed commands pass with no data-root, DB, artifact, or source writes from check mode. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:test]

- [ ] 3.2 Run the six-role implementation review in a new review worktree and resolve every P0 before merge. Objective: validate the actual guard semantics, scope, diagnostics, and operational cost. Inputs: frozen implementation diff and focused test evidence. Outputs: six reports and consensus findings. Affected contracts: developer quality and prospective architecture migration. Validation: all six roles inspect actual code, record source citations, and confirm no unresolved P0. Rollback: correct the implementation in this worktree and rerun focused checks/review. Completion evidence: consensus record identifies zero unresolved P0 and any accepted P1/P2 has a follow-up owner. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]

- [ ] 3.3 Inspect working-tree safety, commit the validated implementation unit, push the feature branch, and open a PR against `master`. Objective: deliver an isolated, reversible prerequisite for the next child. Inputs: passing validation and review evidence. Outputs: intentional commit, remote branch, and pull request. Affected contracts: none beyond reviewed developer tooling. Validation: `git status -sb` confirms only intended files, commit message records tests/compatibility/no-data-risk and includes the required TRAE CLI co-author trailer, project-local GitHub CLI auth/PR flow succeeds after push. Rollback: revert the isolated commit or close the PR; no runtime/data rollback exists. Completion evidence: commit, push, and PR URL are recorded. [validates:architecture.static-guard] [validates:architecture.baseline] [validation:review]
